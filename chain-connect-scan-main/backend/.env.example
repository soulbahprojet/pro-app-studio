# ============================================
# Configuration AI Copilote 224SOLUTIONS
# ============================================

# === OpenAI Configuration ===
# Clé API OpenAI (nécessaire pour la génération de code)
OPENAI_API_KEY=sk-proj-...
# Modèle à utiliser (recommandé: gpt-5-2025-08-07)
OPENAI_MODEL=gpt-5-2025-08-07

# === GitHub Configuration ===
# Token GitHub avec permissions repo (lecture/écriture)
GITHUB_TOKEN=ghp_...
# Organisation/utilisateur propriétaire du repo
GITHUB_OWNER=votre-org
# Nom du repository
GITHUB_REPO=224solutions
# Branche de base pour les PR
BASE_BRANCH=main

# === Sécurité et Authentification ===
# Clé API pour authentifier les requêtes PDG
PDG_API_KEY=secret-pdg-key-ultra-secure
# JWT Secret pour validation des tokens (optionnel)
JWT_SECRET=votre-jwt-secret

# === Configuration Sandbox ===
# Activer/désactiver l'exécution des tests en sandbox
SANDBOX_ENABLED=true
# Timeout pour les tests sandbox (en secondes)
SANDBOX_TIMEOUT_SECONDS=600
# Commandes de test par défaut
DEFAULT_TEST_COMMANDS=npm run test,npm run lint

# === Options de déploiement ===
# Autoriser le merge automatique des PR (DANGER!)
AUTO_MERGE_ALLOWED=false
# Environnement par défaut pour les déploiements
DEFAULT_ENVIRONMENT=staging
# URL de callback pour les notifications de déploiement
DEPLOY_CALLBACK_URL=https://pdg.224solutions.com/api/deploy-callback

# === Configuration serveur ===
# Port du serveur Express
PORT=3001
# Environnement (development/production)
NODE_ENV=development
# URL de base pour les webhooks
BASE_URL=http://localhost:3001

# === Limites et quotas ===
# Limite de taille pour les prompts (caractères)
MAX_PROMPT_LENGTH=10000
# Nombre maximum de fichiers par génération
MAX_FILES_PER_GENERATION=20
# Taille maximum par fichier (bytes)
MAX_FILE_SIZE=100000

# === Monitoring et logs ===
# Niveau de log (debug/info/warn/error)
LOG_LEVEL=info
# Chemin du fichier d'audit
AUDIT_LOG_PATH=./logs/audit-ai.log
# Webhook pour notifications Slack/Discord (optionnel)
NOTIFICATION_WEBHOOK=https://hooks.slack.com/...

# === Cache et performance ===
# Durée de cache pour les réponses AI (secondes)
AI_CACHE_TTL=3600
# Redis URL pour le cache (optionnel)
REDIS_URL=redis://localhost:6379

# === Features flags ===
# Activer la génération de tests automatiques
ENABLE_AUTO_TESTS=true
# Activer l'optimisation de code
ENABLE_CODE_OPTIMIZATION=true
# Activer les métriques de performance
ENABLE_PERFORMANCE_METRICS=true

# ============================================
# Instructions d'installation:
# 
# 1. Copiez ce fichier vers .env
# 2. Remplissez toutes les variables marquées comme obligatoires
# 3. Configurez les permissions GitHub appropriées
# 4. Testez avec: npm run test-config
# ============================================